DATABASE_URL="postgresql://postgres:postgres@localhost:5432/clawcraft?schema=public"

# Dev mode (see src/config/dev-mode.ts)
DEV_TIME_SCALE=360
DEV_MOCK_LLM=true
DEV_SEED=true
DEV_VERBOSE=false

# Optional: OpenRouter LLM (for narrative/content experiments)
# - Never commit real keys. Put them in `.env` / `.env.local` (both are gitignored).
# - Recommended model (cheap + structured outputs): deepseek/deepseek-v3.2
OPENROUTER_API_KEY=""
OPENROUTER_MODEL="deepseek/deepseek-v3.2"
OPENROUTER_BASE_URL="https://openrouter.ai/api/v1"
OPENROUTER_TIMEOUT_MS=25000
